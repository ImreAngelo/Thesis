% \section{BV-RNS Operators}
% Base stats for the BV implementation. \textcolor{green}{Maybe it makes sense to use other parameters in this section?}

\section{Noise Analysis}
\label{sec:results:noise}
This section presents the noise growth of the implemented functions, reported throughout as the max-norm $\|\epsilon\|_\infty = \max_i\{\epsilon_i\}$. The absolute magnitude of the error is of little interest on its own; what matters is how many bits of the modulus it occupies. Every plot in this section is therefore semi-logarithmic with a base-2 $y$ axis, so that a vertical distance reads directly as a number of bits.

\subsection{Core Library}
For the external product, we apply $x_i = x_{i-1} \boxdot A_i$ where $x_0 = \Call{Encrypt}{1}$ using fresh encryptions $A_i = \Call{RGSW}{1}$ each iteration.

\subsubsection{External Product}
Due to the asymmetry discussed in~\autoref{rem:extprod-asymmetry} making the noise additive instead of multiplicative, we expect to see linear noise growth with repeated applications of the external product as long as the \gls{rgsw} remains fresh.

\input{Figures/Plots/bv-external-product.tex}

The measured growth is linear in the length of the chain, and the error gains only a few bits across the full chain at every value of $\ell$. Increasing $\ell$ shifts the whole series downwards without changing its slope.

\subsubsection{Internal product}
For the internal product, we apply $X_i = X_{i-1} \boxtimes A_i$ where $X_0 = \Call{RGSW}{1}$ using fresh encryptions $A_i = \Call{RGSW}{1}$ each iteration.

% \item Maybe: show max chain length if RHS is accumulated noise
\input{Figures/Plots/bv-internal-product.tex}

The growth is again linear in the length of the chain and again shifts downwards with $\ell$. Both the starting error and the amount added per product are substantially larger than for the external product.
% \begin{itemize}
%     \item $\num{\intSlope}\cdot i + \num{\intIntercept} \leq 2^{180} \implies i \leq \num{1.70283e75}$
% \end{itemize}

\subsection{sPAR}
The protocol was run end to end under the multi-party context for $\eta \in \{2, 4, 8\}$, and \autoref{tab:noise:multiparty} lists the largest error found in the message matrix $L$ once every client has written. Decryption fails once the error reaches $\lfloor Q/2 \rfloor$, which is $359$ bits at the parameters of \autoref{tab:bench:params}, and the table reports the remaining headroom against that bound.

% TODO: Multiple (10) runs
\begin{table}[H]
    \caption{Largest error in $L$ after all $\eta$ writes, multi-party context}
    \label{tab:noise:multiparty}
    \centering
    \begin{tabular}{ccc}
        \toprule
        $\mathbf{\eta}$ & \textbf{Error (bits)} & \textbf{Headroom (bits)} \\
        \midrule
        $2$ & $239$ & $120$ \\
        $4$ & $288$ & $71$  \\
        $8$ & $356$ & $3$   \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{itemize}
    \item Graph single-key setting with same parameters here
\end{itemize}

% A second, independent run of the same configuration recorded $241$, $288$ and $334$ bits, so the measurement varies by up to roughly twenty bits with the bucket indices the clients happen to sample. At $\eta = 16$ the protocol no longer decrypts correctly under these parameters.

%% NOTE: Figures/Plots/spar-write reads Data/Noise/write-N16.csv, which still holds
%% pre-operand-swap data from before the parameter change (see write-N2.csv for the
%% current run). Re-include once that series has been regenerated.
% \input{Figures/Plots/spar-write}
