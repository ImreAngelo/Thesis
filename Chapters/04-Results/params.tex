\section{Parameters}
I found that the parameters suggested in \cite{cryptoeprint:2025/860} results in too much noise, and are thus not capable of running the full \gls{spar} protocol without decryption failure. Rather than tweaking these parameters until the protocol could run, I instead aim to fully utilize the \gls{rns} context by using parameters that enable SIMD batching. % The parameteres were designed for TFHE, with Q = 2^64

Parameters were thus selected to provide $\lambda \geq 128$ bits of security at the largest possible $Q$ while supporting SIMD batching ($t$ prime s.t. $t\equiv 1 \bmod{N}$), then $\ell$ was tweaked until the sPAR protocol runs without decryption failure. The RNS primes are set slightly below $64$ bits to minimize $k$, \textcolor{red}{with the first prime $q_0$ being slightly larger than the following primes by convention??} I found that this left much headroom, prompting me to lower $k$ until $\ell = 2$ could no longer sustain the noise. The resulting parameters, listed in~\autoref{tab:bench:params}, yield a security parameter of $339.6$ bits~\cite{cryptoeprint:2015/046} for the underlying \gls{rlwe} scheme.

\begin{table}[H]
    \caption{Base parameters for BV-RNS}
    \label{tab:bench:params}
    \centering
    \begin{tabular}{lc}
        \toprule
        \textbf{Parameter}                  & \textbf{Value}   \\
        \midrule
        decomposition $(\ell, B)$               & $(3, 2^{21})$    \\
        number of RNS moduli $k$                & $4$              \\
        first mod size $\log{q_0}$              & $60$             \\
        scaling mod size $\log{q_i},\; i > 0$   & $59$             \\
        ciphertext modulus order $\log{Q}$      & $2^{296}$        \\
        standard deviation $\sigma$             & $3.19$           \\
        plaintext modulus $t$                   & $65537$          \\ % 2^{16}+1
        polynomial degree $N$                   & $16385$          \\
        security parameter $\lambda$            & $339.6$          \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{itemize}
    % \item Tried to replicate the original parameters exactly, I was not able to run the protocol using the exact parameters from~\cite{cryptoeprint:2025/860} due to noise (even for $\eta=2$). These parameters would be too small to take advantage of RNS anyways, as they all fit inside a computer register $\leq 2^{64}$
    \item Explain difference in size between $q_0$ and $q_i$
    \item Explain $q_i$ are random $59$-bit primes so only their order is listed 
    \item Data per message = $N\log(t)/8 \approx 16385 \cdot (16)/8 = 32770$ bytes $\approx 32$ Kb
    \item sPAR data per msg = $2048 * 8/8 = 2048 = 2$ Kb
    \item For performance, the key is generally to minimize $k\ell$, in this case $2k\ell = 2\cdot  4\cdot 3 = 2\cdot 12 = 24$ rows per RGSW! 
    \item Note for self: if larger params is 16 times slower than expected, throughput is the same
%     \item MOVE TO IMPLEMENTATION DETAILS: For BV, the base $B = \lfloor \max q_i \rfloor$ is chosen automatically so to be the smallest $B$ that covers the largest RNS prime with $\ell$ digits when the program starts, because the $q_i$ are randomly chosen. 
\end{itemize}
