\section{Discussions}
\label{sec:results:discussion}
\begin{itemize}
    \item The results are slower than anticipated. Even though we are using larger parameters than the small TFHE parameters suggested in~\cite{cryptoeprint:2025/860}, it stands to reason that 
\end{itemize}

\begin{itemize}
    % \item Interpretation moved out of \autoref{sec:results:noise}: growth is additive rather than multiplicative because the \gls{rgsw} operand is fresh at every step, the favourable case of \autoref{rem:extprod-asymmetry}; a larger $\ell$ lowers the floor because smaller digits mean a smaller $\beta$, paid for in the $2k\ell$ rows each product must traverse; the internal product governs the supportable circuit depth since it is $2k\ell$ external products stacked.
    \item The write step dominates a round entirely, and it is the one phase that cannot be parallelized across clients.
    \item In the multi-party setting, noise (flooding) caps $\eta \leq 8$ with the current parameters.
    \item Some possible avenues:
    \item AVX512 not tested due to hardware incompatability
    \item SIMD
    \item Throughput angle
    \item \begin{itemize}
        \item Data per message = $N\log(t)/8 \approx 16385 \cdot (16)/8 = 32770$ bytes $\approx 32$ Kb
        \item sPAR data per msg = $2048 * 8/8 = 2048 = 2$ Kb
        \item If larger params is 16 times slower than expected, throughput is the same
    \end{itemize}
\end{itemize}

\begin{itemize}
    \item \textbf{Discussions:} If write can use \gls{simd} batching the write loop can be divided by up to $N \implies 4.675$ ms, $18.798$ ms, $74.69$ ms per round, respectively, but the other functions must also be scaled up.. % works in some scenarios
\end{itemize}
