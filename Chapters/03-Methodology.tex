\chapter{Methodology}
\label{chp:methodology}

% % \section{Notes}
% % In Brakerski's scheme, similar to previous FHE schemes based on LWE, the ciphertext $\overset{_\rightarrow}{c}$ and secret key $\overset{_\rightarrow}{s}$ are $n$-dimensional vectors whose dot product $\langle \overset{_\rightarrow}{c}, \overset{_\rightarrow}{s}\rangle \approx \mu$ equals the message $\mu$, up to some small ``error'' that is removed by rounding.

% \textcolor{red}{\textbf{Move implementation details from \autoref{sec:impl:hps} here?}}

\newcommand{\corelib}{\texttt{core}}
\newcommand{\serverlib}{\texttt{server}}
\newcommand{\clientlib}{\texttt{client}}

%%
%% Introduction - Present code contributions: spar and 
\vfill
OpenFHE does not expose \gls{rgsw} or the external and internal products through its public API. Running \gls{spar} on BGV therefore meant building the products before the protocol.

This work produced two independent artefacts. The \corelib{} library extends OpenFHE~\cite{cryptoeprint:2022/915} with \gls{rgsw} encryption and both products over an \gls{rns} gadget, and is independent from \gls{spar}; any OpenFHE project needing \gls{rgsw} arithmetic can use it as it stands. The \clientlib{} and \serverlib{} libraries implement the protocol on top of it, reaching \corelib{} only through an abstract interface, so a different backend providing the same API would serve equally well.

Section \ref{sec:impl:core} describes the \corelib{} library, from the gadget instantiation and the parallel decomposition through to the two products. Section \ref{sec:impl:spar} describes our instantiation of \gls{spar} and where it departs from the original specification. The final section describes the harness that produces the measurements of \cref{chp:results} --- what is benchmarked, how noise is extracted, and how thread scaling is extrapolated beyond the cores available.
\newpage

\input{Chapters/03-Methodology/structure}
\input{Chapters/03-Methodology/impl-core}
\input{Chapters/03-Methodology/impl-spar}
\input{Chapters/03-Methodology/experimental-setup}