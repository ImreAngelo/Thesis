\section{Residue Number System}
\begin{itemize}
    \item We use large numbers
    \item A computer word is typically 64-bit (or less: 32-bit)
    \item We want to work exclusively with numbers smaller than 64 bits
    \item A \gls{rns} decomposes every number into a set of residues
    \item Addition is easy, but multiplication uses \gls{ntt}
\end{itemize}

\textcolor{red}{\textbf{The gadget becomes the RNS primes?}}


\subsection{CRT representation}
Let 

\begin{equation}
\mathsf{DCRT}(x) = 
\begin{pmatrix}
    a & b \\
    c & d
\end{pmatrix}
\end{equation}

\subsection{Double CRT rep.}

\newpage


%%%
%%% sPAR Algorithm 2
%%%
\begin{algorithm}
\caption{Homomorphic placing $\mathsf{HomPlacing}$ for one slot}
\label{alg:expandRlwe}
\begin{algorithmic}[1]
    \State \textbf{Input:} An encrypted value $V_r$ and $\log{n}$ bits $\{c_0,\dots,c_{L-1}\}$, where $L = \log{n}$
    \State \textbf{Output:} A vector $\mathbf{l}$ consisting of $n$ values of leaves: $z_0, \dots, z_{n-1}$, where $\mathbf{l}[i] = z_i$ for $i \in \{0,\dots, n-1\}$
    
    \State $\mathbf{l} := \mathbf{0}$
    \State Initialize root: $b_0 = V_r$
    \State Initialize the node values: $b_i := 0$ for $i \in \{1,\dots,2n - 2\}$
    
    \For{$i \leftarrow 0,\dots,L - 1$}
        \For{$j \leftarrow 0,\dots,2^i-1$}
            % \State $k := 2^{i+1}+2j$
            \State $r := b[{2^i-1+j}]$
            \State $b_{2^{i+1}+2j} := r\boxdot c_i$
            \State $b_{2^{i+1}+2j-1} := r - r\boxdot c_i$
        \EndFor
    \EndFor
    \For{$i \leftarrow 0,\dots, n - 1$}
        \State $z_i := b_{2^{l+1} - 1 + i}$
        \State $\mathbf{l}[i] = z_i$
    \EndFor
    
    \State // xxx //
    \State Let 
    
    \State\Return $\mathbf{l}$
\end{algorithmic}
\end{algorithm}


\newpage